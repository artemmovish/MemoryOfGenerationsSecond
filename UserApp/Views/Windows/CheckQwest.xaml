<Window x:Class="UserApp.Views.Windows.CheckQwest"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:UserApp.Views.Windows"
        mc:Ignorable="d"
        Title="CheckQwest" Height="197" Width="588">
    <Grid>
        <StackPanel HorizontalAlignment="Center" VerticalAlignment="Center">
            <TextBlock x:Name="QwestText" Text="Вопрос?" TextAlignment="Center" FontSize="16"/>
            <TextBox x:Name="AnswerText" Text="Ответ?" Width="250" Margin="0,15,0,15"/>
            <Button Style="{StaticResource RoundedButtonStyle}" Click="Button_Click">
                <TextBlock Text="Ответить"  FontSize="16" Style="{StaticResource btnText}"/>
            </Button>
        </StackPanel>
    </Grid>
</Window>
